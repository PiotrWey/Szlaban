# DO NOT EDIT!
# This file is dynamically populated by Gradle on build from gradle.properties
# If you want to change any values, change them there! Otherwise this file may
# become broken!
# The only parts you should touch are depend and libraries if the project gains
# new dependencies.

# Plugin Metadata
name: ${name}
version: ${version}
prefix: ${prefix}
description: ${description}
website: ${website}

# Authorship
authors: ${authors}
contributors: ${contributors}

# Permissions and Commands
# ...

# Development and Plugin Loading
api-version: ${api_version}
main: ${main}
depend: []
libraries: []